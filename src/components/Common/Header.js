import React from "react";
import { NavLink } from "react-router-dom";

const Header = () => {
  return (
    <header class="">
      <h1>The complete weather App</h1>
    </header>
  );
};

export default Header;
